# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

font pango:JetBrainsMono Nerd Font 12
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

assign [class="discord"] $ws9

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

hide_edge_borders both
default_border none
default_floating_border none
gaps inner 10
gaps outer 5

set $bg-color																#1F1F28
set $inactive-bg-color											#1F1F28
set $bg-color-workspace											#050506
set $inactive-bg-color-workspace						#050506
set $text-color			          							#C8C093
set $inactive-text-color										#54546d
set $urgent-text-color											#DCA561

# Window Colors
#													border						 background					text 				 	        indicator
client.focused						$bg-color					 $bg-color			$text-color  					$border-color
client.unfocused					$inactive-bg-color $inactive-bg-color	$inactive-text-color	$bg-color
client.focused_inactive		$inactive-bg-color $inactive-bg-color	$inactive-text-color	$bg-color
client.urgent							$bg-color   			 $urgent-bg-color 	$text-color          	$bg-color


# Status bar colors
bar {
        status_command i3blocks -c ~/.config/i3/i3blocks.conf
				tray_output none # Hide the icons on the right side of the bar like wifi and discord icons
				colors {
							# background $bg-color
							background $bg-color-workspace
							separator #54546d
							focused_workspace $bg-color-workspace $bg-color-workspace $text-color
							inactive_workspace $inactive-bg-color-workspace $inactive-bg-color-workspace $inactive-text-color
							urgent_workspace $bg-color-workspace $bg-color-workspace $urgent-text-color
				}
}

# Set wallpaper
exec_always --no-startup-id feh --bg-scale /home/ilyes/wallpapers/wallpaper-1.png --no-fehbg

# Start-up apps
## Graphical Apps
exec --no-startup-id i3-msg 'workspace 2; exec --no-startup-id firefox'
exec --no-startup-id discord

## Background apps
exec --no-startup-id redshift
exec --no-startup-id picom
exec --no-startup-id sxhkd

-- NOTE: keymaps are intentionally NOT required here. `init.lua` loads them
-- after `ilyes.pack` so personal maps take precedence over plugin maps.
require 'ilyes.core.options'
require 'ilyes.core.autocommands'
